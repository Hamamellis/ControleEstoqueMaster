@model List<ControleEstoque.Web.Models.MarcaProdutoModel>

@{
    Layout = "~/Views/Shared/_Cadastro.cshtml";
    ViewBag.Title = "Marcas de Produtos";
    ViewBag.Lista = Model;
}

<div class="row">
    <h2>
        <div class="rowimgmarcasprodutos">
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/corsair.jpg" alt="Corsair" style="width:90%" />
            </div>
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/HyperX.jpg" alt="HyperX" style="width:70%" />
            </div>
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/hp.ico" alt="Hewlett-Packard" style="width:45%" />
            </div>
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/intel.ico" alt="Intel" style="width:45%" />
            </div>
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/pcyes.png" alt="Pcyes" style="width:100%" />
            </div>
            <div class="produtoimgmarcasprodutos">
                <img src="~/img/marcasprodutos/westerndigital.png" alt="Western Digital" style="width:100%" />
            </div>
        </div>
    </h2>
</div>
<hr />

@* - ABAIXO - FIZEMOS O USO DE @RENDERSECTION E REFERENCIAMOS - *@

@section Styles{
    <link href="~/Content/bootstrap-select.css" rel="stylesheet" />
    <link href="~/Content/Cadastro/MarcaProduto.css" rel="stylesheet" />
}

@section TituloGrid {
    <th><span class="coluna-ordenacao" data-campo="nome">Nome:</span></th>
    <th><span class="coluna-ordenacao" data-campo="ativo">Ativo:</span></th>
    <th>Ação:</th>
}

@section CamposCadastro {
    <div class="row">
        <div class="col-md-12">
            @Html.Label("txt_nome", "Nome", new { @class = "control-label" })
            @Html.TextBox("txt_nome", null, new { @class = "form-control" })
        </div>
    </div>
    <div class="row" style="margin-top: 15px">
        <div class="col-md-12">
            <div class="form-check">
                @Html.CheckBox("cbx_ativo", new { @class = "form-check-input" })
                @Html.Label("cbx_ativo", "Ativo", new { @class = "form-check-label" })
            </div>
        </div>
    </div>
}

@section Scripts{
    <script src="~/Scripts/bootstrap-select.js"></script>
    <script>
        var tituloPagina = '@ViewBag.Title';
        var url_excluir = '@Url.Action("ExcluirMarcaProduto", "CadMarcaProduto")';
        var url_confirmar = '@Url.Action("SalvarMarcaProduto", "CadMarcaProduto")';
        var url_page_click = '@Url.Action("MarcaProdutoPagina", "CadMarcaProduto")';
        var url_tam_pag_change = '@Url.Action("MarcaProdutoPagina", "CadMarcaProduto")';
        var url_filtro_change = '@Url.Action("MarcaProdutoPagina", "CadMarcaProduto")';
        var url_alterar = '@Url.Action("RecuperarMarcaProduto", "CadMarcaProduto")';
        var linhas = [];

        @{
            var linha = 0;
            foreach (var item in Model)
            {
                linha++;
                if (linha <= ViewBag.QuantMaxLinhasPorPagina)
                {
                    @:linhas.push(@Html.Raw(Json.Encode(item)));
                }
                else
                {
                    break;
                }
            }
        }

    </script>
    <script id="template-grid" type="x-tmpl-mustache">
        <tr data-id="{{ Id }}">
            <td>{{ Nome }}</td>
            <td>{{ #Ativo }}SIM{{ /Ativo }}{{ ^Ativo }}NÃO{{ /Ativo }}</td>
            <td>
                <a class="btn btn-primary btn-alterar" role="button"><i class="glyphicon glyphicon-pencil"></i> Alterar</a>
                <a class="btn btn-danger btn-excluir" role="button"><i class="glyphicon glyphicon-trash"></i> Excluir</a>
            </td>
        </tr>
    </script>
    <script src="~/Scripts/Cadastro/MarcaProduto.js"></script>
}
